# Application icon

Place a square PNG named `icon.png` in this directory.

The next build will use it for the Windows executable, application window, and
notification-area icon. A transparent image of at least 256×256 is recommended.
If the file is missing or invalid, the mapper keeps its generated window/tray
fallback and the executable keeps its default build icon.
